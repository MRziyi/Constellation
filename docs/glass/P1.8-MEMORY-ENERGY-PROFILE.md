# P1.8 — Memory + Energy Profile + Tighten

**Created**: 2026-05-29 EOD · **Owner**: Zack
**Target repo**: `Constellation-Glass` on `pivot/baremetal-v2.1`
**Companion**: [GLASS-CLIENT-DESIGN.md](GLASS-CLIENT-DESIGN.md) v2.1 · [GLASS-SDK-REFERENCE.md](GLASS-SDK-REFERENCE.md) · [SoT C-37 / C-42](../constitution/SOURCE-OF-TRUTH.md)

---

## 1. 目标

把"能效是 Glass 端 #1 指标"（C-37）从**设计原则**变成**事实**：

1. **量化** Constellation-Glass app 在 Rokid Glasses 上的实际内存 + 能耗 baseline
2. **找出**热点（哪些操作贵、哪些组件长驻、哪些缓存可清）
3. **紧凑化** —— 按 ROI 排序，做掉前 3-5 个最大问题
4. 把 P1.5/P1.6 阶段做的"信念约束"（eager-close、detach-on-Idle、no IMU、no HBM 等）逐一**带数据验证**

完成后 P1.9（多并行 session）才有数据基础决定 pin 上限。

---

## 2. 当前状态（已做 vs 未量化）

### 已做的（设计 + 代码）

| 项 | 状态 | 量化？ |
|---|---|---|
| AudioRecord eager close (C-42) | ✅ 代码已是 | ❌ 没量化 idle drain |
| WakeLock 5min ceiling (C-49) | ✅ | ❌ 没量化 |
| HUD overlay detach-on-Idle (R-10 lesson) | ✅ 真机验过 | ❌ 没量化"attached vs detached" drain delta |
| FGS type 显式声明 (mic\|camera) | ✅ | n/a (correctness) |
| WSS `connectionPool.evictAll()` 15s 超时 | ✅ | ❌ 没量化每次连接 footprint |
| No background mic / wake-word / IMU / HBM | ✅ 全 designed-out | ✅ 信念正确 |
| BT-PAN 作 WiFi-off省电备选 | ✅ 真机走通 | ❌ 跟 WiFi 模式的能耗 delta 未测 |
| HUD refresh ≤ 4 Hz | ✅ 设计目标 | ❌ 真实 refresh 频率未量 |
| Compose runtime + ui-tooling debug | ✅ 集成 | ❌ idle heap footprint 未量 |

### 严重不够的（实测全部缺）

1. 24h 待机功耗（WiFi mode vs BT-PAN mode）
2. Per-cycle 能耗：1 次 voice CARD / 1 次 vision CARD / 1 次 shortcut fire
3. 内存 baseline：cold start / 1h idle / mid-CARD / Listening / post-photo / 5 cycles 后
4. 内存泄漏：LeakCanary 还没集成
5. Compose runtime 在 HUD overlay attached vs detached 时的 heap 差
6. WSS connection footprint（包括 OkHttp pool, json buffers）
7. CameraX 拍照管线峰值（capture session + JPEG encoder + b64 string）
8. AudioRecord 8-ch × 16kHz × 2s 缓冲（理论 512KB but 实际 buffer 大小）
9. WSS 双发：每个 CARD 都发 legacy `Command` + glass-shaped `card`，每张卡 ~2x 带宽
10. JSON 解析/序列化的 allocation churn（kotlinx-serialization）

---

## 3. 实施 — 4 个 Phase

### ⚠ Testing methodology — production flow ONLY

**This is a gotcha that bit us during the first baseline session.**

Per [IN-APP-UI-DESIGN](IN-APP-UI-DESIGN.md) and Zack's stated intent: the
**MainActivity is for settings only** (QR pair, Wi-Fi adjustment, see status
once). All runtime interaction happens on the **HUD overlay** that floats
above the Sprite launcher (or whatever else is the foreground Activity).

When testing memory/energy:
- ❌ **Don't** keep MainActivity in the foreground. `am start MainActivity` is
  fine to bootstrap the Service one time, but **immediately send HOME**
  (`adb shell input keyevent KEYCODE_HOME`) so the Sprite launcher resumes.
- ✅ **Verify** with `dumpsys activity activities | grep topResumedActivity` —
  should be `com.rokid.os.sprite.launcher/.main.SpriteMainActivity`, not our app.
- ✅ **Trigger CARDs** while MainActivity is backgrounded — that's the real
  production flow.
- ✅ **Screencaps then correctly show** the HUD overlay floating above Sprite
  launcher (verified 2026-05-29 17:13: `Battery status` card with rounded
  green CardFrame visible above the Sprite "Hi Rokid" prompt).

If a screencap shows MainScreen content, it means MainActivity is in the
foreground and the overlay is being z-order-occluded — **the measurement is
invalid for "what the user actually sees during normal use"**.

---

### Phase α — Tooling（半天）

集成观测工具，**不改业务逻辑**。

#### α.1 LeakCanary (debug-only)

加 `debugImplementation("com.squareup.leakcanary:leakcanary-android:2.14")`。
只在 `*Debug` build 生效 — 不影响 release APK 体积。
LeakCanary 监听 `Activity` / `Fragment` / `ViewModel` / `Service` / Custom
watchers。我们关心：
- ✅ `ConstellationService`（在 onDestroy 时应被 GC）
- ✅ `MainActivity` / `LoginScreen` / `ConnectScreen` 等
- ⚠️ Custom watcher 需要给 `GlassHudOverlay` + `OverlayHostOwner` 加（不是 standard
  Android 组件，LeakCanary 默认看不到）
- ⚠️ `StateMachine` / `WssClient` / `AudioPipeline`（Long-lived singletons in scope；
  不该被 GC，**反向检查 — 验证它们没被意外重建**）

#### α.2 Memory & Process Capture Script

`scripts/glass-profile.sh`（在 Constellation 仓库的 `scripts/` 下，方便跨 session 复用）：

```bash
#!/usr/bin/env bash
# Capture memory + cpu + battery + WSS state from Rokid Glasses
# Usage: ./glass-profile.sh <label>
# Output: profile-out/<label>-<ts>.{meminfo,top,battery,dumpsys-power,logcat}
set -e
DEV=${ADB_DEV:-<glass-serial>}
LABEL=${1:-snapshot}
TS=$(date +%Y%m%d-%H%M%S)
OUT=profile-out/${LABEL}-${TS}
mkdir -p "$OUT"
echo "== meminfo =="
adb -s $DEV shell dumpsys meminfo com.constellation.glass > "$OUT/meminfo.txt"
echo "== top (5 samples, 1s apart) =="
adb -s $DEV shell top -n 5 -d 1 -b -p $(adb -s $DEV shell pidof com.constellation.glass) > "$OUT/top.txt" 2>&1 || true
echo "== battery stats =="
adb -s $DEV shell dumpsys battery > "$OUT/battery.txt"
adb -s $DEV shell dumpsys batterystats --reset > /dev/null  # for next interval
echo "== power state =="
adb -s $DEV shell dumpsys power | head -100 > "$OUT/power.txt"
echo "== wakelock =="
adb -s $DEV shell dumpsys power | grep -A2 "Constellation\|com.constellation" > "$OUT/wakelock.txt" || true
echo "== app process info =="
adb -s $DEV shell dumpsys activity processes | grep -A 30 "com.constellation" > "$OUT/process.txt" || true
echo "Saved → $OUT/"
```

#### α.3 In-app heap snapshot trigger (debug-only)

`MainActivity` debug menu 加 "Dump heap" 按钮 → 调 `Debug.dumpHprofData(path)`。
不在生产 build 启用。`scripts/glass-profile.sh dump-heap` 可触发并 pull。

#### α.4 Done-when

- `./scripts/glass-profile.sh cold-start` 跑出 5 个 txt 文件
- LeakCanary 在 OnePlus 9 + Rokid Glasses 都安静跑 1 小时无 false positive
- in-app heap dump 能拉到本地 `.hprof`

---

### Phase β — Baseline 测量（1 天）

固定操作矩阵，每项做 3 次取中位数。

| 测量点 | 怎么触发 | 关注指标 |
|---|---|---|
| **Cold start** | `am force-stop` + `am start MainActivity` + 等 10s | meminfo PSS / Java heap / Native heap / Total RSS |
| **1h idle (HUD off)** | 启动后等 1h 不操作 | top RAM 漂移 / 是否有 wakelock 持续 / 电池 % 减少 |
| **Voice cycle (no vision)** | Halo Ring tap → "battery status?" → CARD → 等关 | per-cycle 峰值 + GC 次数 + Δ-battery |
| **Vision cycle** | Halo Ring tap → "describe what you see in front of me" → CARD → 等关 | 同上 + CameraGate 启动到关闭的峰值 |
| **Shortcut fire (whats-in-front)** | `am broadcast` shortcut → CARD | 同 voice vision |
| **5 cycles back-to-back** | 上面 4 个混合 × 5 | 检查内存是否单调上涨 (= 泄漏) |
| **HUD overlay attached (Card on)** | 触发任意 CARD + 持续 30s | 跟 HUD detached 时 baseline 对比 |
| **WSS pool size** | 已连接 1h 后 dumpsys + scratch class loader 反射 | OkHttp ConnectionPool 实例数, 连接数 |

#### 输出

写到 `docs/glass/P1.8-MEMORY-ENERGY-PROFILE.md` 末尾新增 §6 **"Baseline measurements (2026-05-XX)"**。每项三列：cold-start / 1h-idle / per-cycle。

#### Done-when

- 所有 8 个测量点跑过 + 数字写进 doc
- 至少 1 个明显热点被识别（候选：WSS 双发、Compose tooling debug 占用、AudioRecord buffer 大小、JSON serializer churn）

---

### Phase γ — 24h drain（⏸ 推迟到不熬夜的 session）

需要：
- 眼镜挂着电池监视器跑整夜
- 第 1 晚：WiFi mode 全程
- 第 2 晚：BT-PAN mode 全程（iPhone LTE）
- 早上读 `dumpsys batterystats --history` 拉时间序列

**当前 session 不做**。Phase β 完成后单独安排。

---

### Phase δ — 紧凑化（基于 β 数据，ROI 排序）

#### 候选改动（先列出，等 β 数据后决定优先级）

| 候选 | 估算节省 | 实施代价 | 风险 |
|---|---|---|---|
| **WSS 双发瘦身** — 当 peer 接受 glass-shape 时 Cortex 不发 legacy Command | ~50% WSS in-traffic | Cortex 端改 `_send_command` | 中（影响 web HUD; 需要兼容） |
| **Compose Bom 升级 + ui-tooling debug-only** | ~5MB APK + 10-20MB runtime | 已是 debug-only；进一步剔 unused module | 低 |
| **JSON 解析复用 `Json` instance** | GC 压力下降 | kotlinx-serialization 标配 | 低 |
| **AudioRecord buffer 大小调** | 工作内存 ↓ | 改 `minBufferSize` 倍数 | 中（音频质量风险）|
| **HUD overlay attached → detached 提前** | idle 内存 ↓ | StateMachine.transitionTo 加 idle 推 detach | 低 |
| **CameraX `setImageQueueDepth(1)`** | 拍照峰值 ↓ | 单行改 | 低 |
| **WSS keepalive cadence 增大** | 网络 wake 频率 ↓ | 改 OkHttp pingInterval 从 15s → 30s 测 | 中（断线重连延迟变大） |
| **Cortex `_pending_image_requests` 老 Future 清理** | 内存 leak 防御 | 已有 `pop` in finally；double-check | 低 |

#### Done-when

每个 δ 改动一个 commit，每个独立 revertible。改完后 β 测量重跑一次确认改善。

---

## 4. 风险 & 回滚

| 风险 | 应对 |
|---|---|
| Phase α LeakCanary 集成阻碍正常使用 (大型 hprof 占空间) | 限制只在 *Debug build；release build 完全不打入 |
| Phase β 测量没有 baseline 之前的旧版本对比，怎么知道改善是真的 | β 完成时打 git tag `p1.8-baseline-2026-05-29`；δ 改完后用同样脚本量；diff 显示 |
| Phase β 量出来发现内存巨大不符预期 (e.g. > 250MB PSS) | 那就是大问题 — 紧急修，不进 δ 的 ROI 排序 |
| Compose 整体内存量过大无法优化 | 灰度 fallback：在 release flavor 把某些屏幕换回 Views（保留 Compose 给 HUD overlay） |
| 24h 测试不可重现因为 BT-PAN 跌断 | Phase γ 加 fallback 脚本：BT 断 → 切 WiFi → 继续记录 |

---

## 5. 完成定义

✅ Phase α: tooling 集成；LeakCanary + capture-script 跑 1h 安静无 false positive
✅ Phase β: 8 个测量点全跑 + 数字写进 §6
✅ Phase β: 至少 1 个被识别为热点（实际中可能更多）
✅ Phase δ: 至少 3 个改动 landed + β 测量证明改善（前后对比表）

⏸ Phase γ (24h drain): 推到下个不熬夜的 session

完成后 P1.9 (session router) 才有数据支撑"并行 session 上限"决策。

---

## 6. Baseline measurements

### 6.1 Rokid Glasses (`<glass-serial>`) — first capture 2026-05-29 17:04-17:07

**Build**: `glass` flavor, debug, includes Compose tooling-debug + LeakCanary + ML Kit barcode.

| 测量点 | TOTAL PSS | Java | Native | Code | SWAP PSS | RSS | 备注 |
|---|---|---|---|---|---|---|---|
| cold-start (t=12s) | **95.5 MB** | 13.2 | 6.0 | 55.7 | 0.9 | 161.5 | service started + WSS connected + IDLE |
| +60s idle (HUD off) | **108.1 MB** | 13.5 | 6.3 | 64.9 | 0.5 | 166.9 | +12.6 MB; mostly Code section growth (deferred class loading); Java + Native stable |
| mid-vision-cycle | **89.6 MB** | 18.7 | 9.2 | 29.4 | **13.9** | 131.7 | CameraGate active; JPEG + b64 + JSON in flight. Kernel started swapping Code pages out (SWAP +13.4 vs cold-start) |
| +30s after vision CARD | **77.3 MB** | 9.6 | 3.5 | 32.3 | 9.9 | 124.5 | Java + Native **below** cold-start — major GC fired cleanly. HUD overlay detached (R-10 lesson). Kernel kept ~10MB code in swap |

Battery: held at 100% throughout (cold → +6min); battery readout granularity is 1% so Δ doesn't appear in this duration. Temperature 28.0°C → 28.5°C (negligible from one vision cycle).

CPU sample (cold-start +12s): 5.0% of one core; %MEM 8.9%.

### 6.2 Findings

✅ **Per-vision-cycle Java heap delta: +5.5 MB peak (13.2 → 18.7)**. Includes ~150 KB JPEG bytes + 201 KB b64 string + JSON serialization buffers. After cycle, Java drops to 9.6 MB (**below** cold-start) — GC is healthy.

✅ **Native heap delta: +3.2 MB during capture (6.0 → 9.2)**. CameraX + JPEG encoder. Returns to 3.5 MB after — also below cold-start.

✅ **No obvious leak on single cycle** — both heaps return below cold-start after the CARD dismisses + HUD overlay detaches (R-10 lesson at work).

⚠️ **Code section is volatile** (55.7 → 64.9 → 29.4 → 32.3). Backed by page cache; kernel evicts under pressure. This is fine; just means PSS varies a lot session-to-session. **Headline number**: ~60 MB code in non-swapped state — driven by Compose ui-tooling-debug + ML Kit barcode + CameraX. Debug-build APK is **58 MB** (release would drop ui-tooling + LeakCanary; ~40-45 MB estimated).

⚠️ **SWAP PSS grew to 14 MB during vision cycle** — kernel is using zram to make room. On an Android Go device with limited RAM this is the normal behavior; just means peak memory pressure happens during vision. Not a problem but watch trajectory across 5+ cycles.

### 6.2.5 FGS keepalive investigation (2026-05-29 17:14 → 17:40)

**TL;DR**: Found 2 distinct issues + landed 1 fix, characterizing the 2nd.

#### Issue 1 (FIXED): `Application.onCreate` BAL violation

`ConstellationApp.onCreate()` was calling `ConstellationService.start(this)`
unconditionally on every process cold-start. **Per Android 12+ Background
Activity Launch (BAL) rules, Application.onCreate runs in a "background"
context** (the process is `pre-top-activity` per ActivityManager logs).
Calling `startForegroundService` from there is allowed, but the subsequent
`startForeground` is silently denied — `allowStartForeground=-1` in dumpsys,
`isForeground=false` despite the notification being posted, `infoAllowStartForeground=null`
(no recorded reason — denied by the BAL gate).

Result: process placed at PERCEPTIBLE_APP_ADJ (200) instead of
FOREGROUND_SERVICE_ADJ (100/125), killable under any memory pressure.

**Fix**: removed `Service.start` from `ConstellationApp.onCreate`; moved to
`MainActivity.onCreate` (which runs in TOP/RESUMED state, gets BAL exemption).
`BootReceiver` keeps its own call — gets the special Android 12+ boot-grace-period exemption.

**Verified post-fix**: dumpsys shows
```
allowStartForeground=12  (was -1)
infoAllowStartForeground=[... uidState: TOP ; code:PROC_STATE_TOP ; targetSdkVersion:32]
isForeground=true        (was false)
foregroundNoti flags 0x62 (now includes FLAG_FOREGROUND_SERVICE; was 0x2)
oom_score_adj=0 (FOREGROUND_APP_ADJ, while MainActivity is visible)
```

Also added `FOREGROUND_SERVICE_TYPE_DATA_SYNC` to the FGS type declaration
(manifest + ServiceCompat.startForeground call) since the persistent WSS
keepalive is semantically a dataSync, not mic/camera (which are eager-close
per C-42). This is the principled type for our use case independent of
the BAL fix.

#### Issue 2 (CHARACTERIZING): Rokid Glasses YodaOS-Sprite demotes FGS on Activity-HOME

After fixing Issue 1, observed that:
- MainActivity visible → `oom_score_adj=0`, `isForeground=true`
- HOME pressed (MainActivity backgrounded) → `oom_score_adj=200`, `isForeground=false`

**Standard Android 12 does not do this**. A foreground service that has
called `startForeground` successfully should retain FOREGROUND_SERVICE_ADJ
(100-125) independent of any Activity state. The vendor mod (YodaOS-Sprite)
is downgrading the FGS to PERCEPTIBLE when the parent Activity backgrounds.

Whether this DEMOTION actually causes kills (vs just being annotation noise)
is being measured by the keepalive probe (running at time of writing).

If demotion → kills under memory pressure → expected workarounds (ordered
by user-friction):
- a) **Add `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` permission** + intent to
  send user to Settings UI for one-tap whitelist grant. Bypasses Doze + vendor
  restrictions. **Tracked as next step if kills observed**.
- b) Keep a transparent zero-UI "ghost Activity" alive in background to mask
  the activity-state check. Complicated; fights the platform.
- c) Document as a Rokid OEM limitation; require user to manually keep the
  app in "background lock" via vendor settings. User-hostile.

If demotion is benign (no kills observed in 20-min probe) → log finding but
no fix needed. The adj=200 state will be killed only if RAM is critically
short, which isn't the typical scenario for this single-app eyewear.

#### Characterization result (2026-05-29 17:36 → 17:50, ~14 min)

**Vendor demotion is BENIGN in tested conditions** — no kill observed across:

- 14 minutes of HOMEd idle with parent Activity backgrounded
- 1 mid-run vision CARD cycle (full Thinking → Card → Idle → adj returned to background)
- 1 mid-run "battery status" CARD cycle (info-only, 6.4s TTL)
- Camera-app launch + back (significant memory churn)
- HOME press after camera

Trajectory of `oom_score_adj` over the run:
- t+0..330s: adj=200 (PERCEPTIBLE_APP_ADJ), isForeground=false, notif=y
- t+~330s: vision Card cycle → adj briefly to FGS level → settled back
- t+~366s onwards: adj=700 (PREVIOUS_APP_ADJ), notif=n (vendor cleared the
  FGS notification), process still alive
- Camera-app launch / back / HOME — no kill, adj stayed 700

So the earlier kill observed at 17:14 was likely a **transient** (just-installed
state, post-`am force-stop` cleanup, system reclaiming memory for install
operation). Under normal idle + light memory churn, the process survives at
adj=700 indefinitely (at least within 14 min).

**Decision**: ship the BAL fix (Issue 1) as the production-critical change.
**Optional defense-in-depth follow-up** (P1.9 or later — not blocking):
add `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` permission + first-run intent to
let user one-tap whitelist the app for Doze + standard standby exemptions.
Won't directly affect the vendor adj demotion but will help under longer
idle (Doze kicks in after ~hour of no interaction).

Side note: the keepalive probe captured a useful observation — Rokid's LMK
appears to be **not aggressive** even at adj=700 absent severe memory
pressure. Concretely it survived camera-app launch (which allocates significant
RAM for the camera2 pipeline). The 8250 SoC's 4GB+ RAM headroom on a
single-app eyewear deployment seems to give us substantial slack.

### 6.3 Still needed (next session)

| Pending measurement | Why it matters |
|---|---|
| 5 cycles back-to-back (voice + vision + shortcut) | Confirm no per-cycle leak accumulation |
| 1h idle (HUD never on) | Validate the Compose runtime + WSS keepalive idle cost |
| 24h drain WiFi mode | Real number for C-37 budget |
| 24h drain BT-PAN mode | Quantify the BT-PAN省电 claim |
| LeakCanary observations after 1h+ use | Catch any silent leaks (overlay views, OkHttp connections, etc.) |
| HUD overlay attached-vs-detached idle delta | Validate the R-10 detach-on-Idle ROI |

### 6.4 Ranked hotspots (preliminary, will firm up after more cycles)

Based on first baseline:

1. **Compose ui-tooling-debug** — adds ~5-10 MB to Code section. Already debug-only, but worth verifying it's stripped from release build via `assembleGlassRelease` once available.
2. **ML Kit barcode scanning** — 390 KB tflite model + classes ~3 MB. Used only on QR-pair Login screen. **Candidate**: lazy-load via on-demand DexClassLoader, OR replace with ZXing (smaller + offline) if barcode usage truly is one-shot.
3. **Per-cycle Java churn** — 5.5 MB peak. JPEG bytes + b64 string is the dominant cost. **Candidate**: stream b64 encoding instead of building a single string (cuts peak by ~50%; complicates the WSS send path).
4. **WSS double-emit** — Cortex sends both legacy `Command` + glass-shaped `card` for every CARD. Glass swallows legacy as no-op (since R-12 fix `edfb3ba`). **Candidate**: Cortex `_send_command` skips legacy emit when peer accepts `card`. ~30-40% in-traffic reduction; zero glass-side cost.

---

## 7. After P1.8 — P1.9 session router 怎么用数据

P1.8 量出来"1 个活 session 占 X MB / Y mAh/h"之后：

- 算出眼镜可用内存 - app baseline = 还剩多少给"额外 session"
- 算出每个 idle session 的 mAh/h × 期望 pin 时长（小时） = 单 session 待机电池预算
- 取 min(内存上限, 电池上限) = 推荐 pin 上限

无 P1.8 数据，P1.9 只能拍脑袋定 pin = 2 或 3，可能跑两天发现眼镜过早没电。
